// src/components/Booking/SeatMap.js
import React from 'react';

const SeatMap = ({ onSeatSelect, selectedSeats, onNext }) => {
  // 在這裡實現座位圖邏輯
  return (
    <div className="seat-map">
      {/* 座位選擇界面 */}
    </div>
  );
};

export default SeatMap;